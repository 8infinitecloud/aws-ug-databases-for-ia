# Core AWS SDKs
boto3>=1.35.0
botocore>=1.35.0

# LangChain ecosystem — pin minor versions; LangChain breaks APIs between minors
langchain==0.3.7
langchain-aws==0.2.6
langchain-community==0.3.7
langchain-core==0.3.15

# PostgreSQL + pgvector
psycopg2-binary>=2.9.9
pgvector>=0.3.2
sqlalchemy>=2.0.36

# Redis (session memory)
redis>=5.1.1

# Streamlit UI
streamlit>=1.40.0
plotly>=5.24.0

# Document parsing (requerido por UnstructuredMarkdownLoader en loader.py)
unstructured[md]>=0.15.0

# Utilities
python-dotenv>=1.0.1
pydantic>=2.9.0
tenacity>=9.0.0        # retry logic for Bedrock throttling
rich>=13.9.0           # pretty terminal output during ingestion

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
